Use `yarn` as dependency management, `native-base` as main UI framework

## Usage

- Fork/clone the repo.
- Install node module using:

```console
yarn
```

-Start the application using
`yarn start` or `expo start`
